# Optional: only needed for gated/private HuggingFace models or to raise
# download rate limits. The default model (Qwen/Qwen2.5-1.5B-Instruct) is
# public and works without a token.
HUGGINGFACEHUB_API_TOKEN=
